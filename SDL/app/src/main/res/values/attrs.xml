<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="SlideLockView">
        <attr name="lock_drawable" format="reference"/>
        <attr name="lock_radius" format="dimension|reference"/>
        <attr name="lock_tips_tx" format="string|reference"/>
        <attr name="locl_tips_tx_size" format="dimension|reference"/>
        <attr name="lock_tips_tx_color" format="color|reference"/>
        <attr name="lock_update_bg_color" format="color|reference"/>
    </declare-styleable>

    <declare-styleable name="PlaneControlView">
        <attr name="control_bg" format="reference"/>
        <attr name="control_rocker" format="reference"/>
        <!--球初始化的位置 0-居中 1-底部 -->
        <attr name="rocker_gravide" format="integer"/>
        <!--球回归点 0-回归中点  1-会回归y轴-->
        <attr name="rocker_back_mode" format="integer"/>
    </declare-styleable>

</resources>